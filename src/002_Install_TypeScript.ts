export function test() {
 /**=======================
  * *       INFO
  * * In this class we learn to
  * * install TypeScript
  *========================**/
 //the better way to install typescript would be 'npm install -g typescript'
 //then use 'tsc filepath.ts //NOTE - This will create the javascript file linked with the indicated typescript.
}
